#include <iostream>

using namespace std;
#include <vector>

int main()
{
    vector<int> pisteet;
    pisteet.emplace_back(1);
    pisteet.emplace_back(2);
    pisteet.emplace_back(3);

    vector<int>::iterator it = pisteet.begin();

    for()

    cout << "Hello World!" << endl;
    return 0;
}
